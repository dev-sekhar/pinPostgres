{
    "name": "pim-postgres-monorepo",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "dev": "yarn workspace @pim/validation generate && concurrently -n BACKEND,FRONTEND \"yarn workspace backend dev\" \"yarn workspace frontend dev\""
    },
    "devDependencies": {
        "concurrently": "^10.0.3"
    }
}